// Placeholder — Steven owns this file (camera feed + AR overlay). Replace freely.
export function WorldView() {
  return (
    <div className="flex h-full w-full items-center justify-center bg-bg-0">
      <span className="text-[11px] uppercase tracking-[0.14em] text-fg-2">World View — awaiting camera feed</span>
    </div>
  );
}
